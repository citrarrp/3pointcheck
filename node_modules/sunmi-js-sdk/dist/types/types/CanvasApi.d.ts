import { BaseStyle } from "../class/BaseStyle";
import { TextStyle } from "../class/TextStyle";
import { BarcodeStyle } from "../class/BarcodeStyle";
import { QrStyle } from "../class/QrStyle";
import { BitmapStyle } from "../class/BitmapStyle";
import { AreaStyle } from "../class/AreaStyle";
interface ICanvasApi {
    /**
     * 画布设置
     * 指定打印标签的画布大小、基础格式
     *
     * @memberof ICanvasApi
     */
    initCanvas: (baseStyle: BaseStyle) => void;
    /**
     * 绘制文本内容
     * 向画布内指定区域绘制文本内容
     *
     * @memberof ICanvasApi
     */
    renderText: (text: string, textStyle: TextStyle) => void;
    /**
     * 绘制条形码内容
     * 向画布内指定区域绘制条形码内容
     *
     * @memberof ICanvasApi
     */
    renderBarCode: (code: string, barcodeStyle: BarcodeStyle) => void;
    /**
     * 绘制二维码内容
     * 向画布内指定区域绘制二维码内容
     *
     * @memberof ICanvasApi
     */
    renderQrCode: (test: string, qrStyle: QrStyle) => void;
    /**
     * 绘制图像
     * 向画布内指定区域绘制图像
     *
     * @memberof ICanvasApi
     */
    renderBitmap: (bitmap: string, bitmapStyle: BitmapStyle) => void;
    /**
     * 绘制特殊图形
     * 向画布指定区域内绘制图形
     *
     * @memberof ICanvasApi
     */
    renderArea: (areaStyle: AreaStyle) => void;
    /**
     * 打印绘制的内容
     * 如果画布内容有效将指定打印机打印画布内容
     *
     * @memberof ICanvasApi
     */
    printCanvas: (count: number, callback: (resultCode: number) => void) => void;
}
export default ICanvasApi;
