interface ICommandApi {
    /**
     * 发送ESC命令
     *
     * @param {ArrayBuffer} esc ESC命令
     * @memberof CommandApi
     */
    sendEscCommand: (esc: string[]) => void;
    /**
     * 发送TSPL命令
     *
     * @param {ArrayBuffer} tspl TSPL命令
     * @memberof CommandApi
     */
    sendTsplCommand: (tspl: string) => void;
}
export default ICommandApi;
