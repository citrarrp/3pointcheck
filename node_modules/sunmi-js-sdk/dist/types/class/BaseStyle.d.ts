import { Align, RenderColor } from "../enum";
export declare class BaseStyle {
    width: number;
    height: number;
    posX: number;
    posY: number;
    align: number;
    renderColor: number;
    constructor();
    /**
     * 设置之后打印内容整体的对齐方式
     * 行打印中设置行内相对位置
     * 图打印中设置无效
     */
    setAlign(align: Align): this;
    /**
     * 设置打印区域宽
     * 行打印设置打印区域宽度
     * 图打印中设置预绘制图宽
     */
    setWidth(width: number): this;
    /**
     * 设置打印区域高
     * 行打印中设置行高
     * 图打印中设置预绘制图高
     */
    setHeight(height: number): this;
    /**
     * 设置打印区域坐标位置
     * 行打印中设置打印区域左边距
     * 图打印中设置预绘制图相对物理打印区域的坐标位置
     */
    setPosX(offset: number): this;
    /**
     * 设置打印区域坐标位置
     * 行打印中设置无效
     * 图打印中设置预绘制图相对物理打印区域的坐标位置
     */
    setPosY(offset: number): this;
    /**
     * 设置打印颜色
     */
    setRenderColor(renderColor: RenderColor): this;
    static getStyle(): BaseStyle;
}
