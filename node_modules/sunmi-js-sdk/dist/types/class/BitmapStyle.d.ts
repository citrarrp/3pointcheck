import { Align, ImageAlgorithm } from "../enum";
export declare class BitmapStyle {
    style: number;
    value: number;
    width: number;
    height: number;
    posX: number;
    posY: number;
    align: number;
    rotate: number;
    constructor();
    /**
     * 设置渲染图片方式
     */
    setAlgorithm(algorithm: ImageAlgorithm): this;
    /**
     * 设置算法浮动值
     */
    setValue(value: number): this;
    /**
     * 如果设置了宽度将改变生成图片后的范围，缩放为指定宽度
     */
    setWidth(width: number): this;
    /**
     * 如果设置了高度将改变生成图片后的范围，缩放为指定高度
     */
    setHeight(height: number): this;
    /**
     * 行打印中设置相对前部分内容的位置
     * 图打印中设置图片内容的x坐标
     */
    setPosX(offset: number): this;
    /**
     * 行打印中设置无效
     * 图打印中设置图片内容的y坐标
     */
    setPosY(offset: number): this;
    /**
     * 行打印可设置基于一行的对齐方式
     * 图打印中设置无效
     */
    setAlign(align: Align): this;
    static getStyle(): BitmapStyle;
}
