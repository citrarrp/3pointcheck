import ICommandApi from "../types/CommandApi";
import { SocketManager } from "./socket";
declare class CommandApi implements ICommandApi {
    socket: SocketManager;
    constructor(socket: SocketManager);
    sendEscCommand: (esc: string[]) => Promise<unknown>;
    sendTsplCommand: (tspl: string) => Promise<unknown>;
}
export default CommandApi;
